# hw2c runtime dependencies (mirrors pyproject.toml [project.dependencies])
pyyaml>=6.0
jinja2>=3.0
pydantic>=2.0
libcst>=1.0.0

# Test dependencies (mirrors pyproject.toml [project.optional-dependencies].dev)
pytest>=7.0.0
pytest-cov>=4.0
