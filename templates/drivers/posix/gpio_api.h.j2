/**
 * @file  gpio_api.h
 * @brief POSIX-style GPIO driver interface (generated by hw2c)
 *
 * All component code MUST use this interface instead of HAL calls.
 * Auto-generated — do not edit manually.
 */

#ifndef __GPIO_API_H
#define __GPIO_API_H

#include <stdint.h>

#ifdef __cplusplus
extern "C" {
#endif

/* ---- Opaque handle ---- */
typedef struct gpio_dev *gpio_handle_t;

/* ---- Configuration ---- */
typedef struct {
    uint8_t mode;   /* 0=input, 1=output, 2=af, 3=analog */
    uint8_t pull;   /* 0=none, 1=up, 2=down */
    uint8_t active_low; /* 0=active_high, 1=active_low */
} gpio_cfg_t;

/* ---- IOCTL commands ---- */
#define GPIO_IOCTL_SET_MODE 0x01
#define GPIO_IOCTL_SET_PULL 0x02
#define GPIO_IOCTL_TOGGLE   0x03

/* ---- API ---- */

gpio_handle_t gpio_open(const char *name, const gpio_cfg_t *cfg);
int32_t       gpio_read(gpio_handle_t h);
int32_t       gpio_write(gpio_handle_t h, int32_t value);
int32_t       gpio_ioctl(gpio_handle_t h, uint32_t cmd, void *arg);
void          gpio_close(gpio_handle_t h);

#ifdef __cplusplus
}
#endif

#endif /* __GPIO_API_H */
