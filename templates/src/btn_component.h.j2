{#
btn_component.h.j2 - Button component public API

Auto-generated — do not edit manually.
#}
/**
 * @file  btn_component.h
 * @brief Button component — multi-instance gesture detector (generated by hw2c)
 *
 * Supports multiple button instances, each bound to a GPIO EXTI pin.
 * Each instance runs an independent 3-state gesture detection machine:
 *   SHORT_PRESS / DOUBLE_PRESS / LONG_PRESS
 *
 * Gesture events are published to the FreeRTOS event_queue as:
 *   EVENT_BUTTON_SHORT_PRESS, EVENT_BUTTON_DOUBLE_PRESS, EVENT_BUTTON_LONG_PRESS
 */

#ifndef __BTN_COMPONENT_H
#define __BTN_COMPONENT_H

#include <stdint.h>

#ifdef __cplusplus
extern "C" {
#endif

/* Number of button instances (auto-discovered from hardware YAML) */
#define BTN_INSTANCE_COUNT  {{ btn_pins | length }}u

#ifdef __cplusplus
}
#endif

#endif /* __BTN_COMPONENT_H */
