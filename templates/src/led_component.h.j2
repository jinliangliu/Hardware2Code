{#
led_component.h.j2 - LED component public API

Auto-generated — do not edit manually.
#}
/**
 * @file  led_component.h
 * @brief LED component — multi-instance LED pattern driver (generated by hw2c)
 *
 * Supports multiple LED instances, each bound to a GPIO pin.
 * Patterns: 0=OFF, 1=FAST_BLINK, 2=SLOW_BLINK, 3=FAULT.
 *
 * Usage:
 *   led_set_pattern_by_index(0, 1);  // first LED → FAST_BLINK
 *   led_set_pattern_by_name("LED", 2); // LED named "LED" → SLOW_BLINK
 */

#ifndef __LED_COMPONENT_H
#define __LED_COMPONENT_H

#include <stdint.h>

#ifdef __cplusplus
extern "C" {
#endif

/* Number of LED instances (auto-discovered from hardware YAML) */
#define LED_INSTANCE_COUNT  {{ led_pins | length }}u

/* LED pattern definitions */
#define LED_PATTERN_OFF         0u
#define LED_PATTERN_FAST_BLINK  1u
#define LED_PATTERN_SLOW_BLINK  2u
#define LED_PATTERN_FAULT       3u
#define LED_PATTERN_ON          4u   /* steady on (driven by led_step) */

/**
 * @brief  Set LED pattern by instance index
 * @param  index    LED instance index (0..LED_INSTANCE_COUNT-1)
 * @param  pattern  Pattern: 0=OFF, 1=FAST, 2=SLOW, 3=FAULT, 4=ON
 */
void led_set_pattern_by_index(uint32_t index, uint32_t pattern);

/**
 * @brief  Set LED pattern by pin label name
 * @param  name     Pin label from hardware.yaml (e.g., "LED", "LED_ERROR")
 * @param  pattern  Pattern: 0=OFF, 1=FAST, 2=SLOW, 3=FAULT
 */
void led_set_pattern_by_name(const char *name, uint32_t pattern);

/**
 * @brief  Toggle an LED once (for legacy toggle_led action)
 * @param  index  LED instance index
 */
void led_toggle(uint32_t index);

/**
 * @brief  Toggle between steady ON and OFF patterns (CLI "led toggle")
 * @param  index  LED instance index
 */
void led_toggle_pattern(uint32_t index);

#ifdef __cplusplus
}
#endif

#endif /* __LED_COMPONENT_H */
