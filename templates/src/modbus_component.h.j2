{#
    modbus_component.h.j2 - Modbus RTU slave component public API
#}
/**
 * @file  modbus_component.h
 * @brief Modbus RTU slave component (generated by hw2c)
 *
 * Wraps drv_modbus + drv_rs485 as a framework component.  Polls incoming
 * Modbus frames in step(); holding registers mirror pub/sub topics.
 */

#ifndef __MODBUS_COMPONENT_H
#define __MODBUS_COMPONENT_H

#include <stdint.h>
#include "component_registry.h"

#ifdef __cplusplus
extern "C" {
#endif

int  {{ comp_name }}_init(component_t *c, void *cfg);
void {{ comp_name }}_step(component_t *c);
void {{ comp_name }}_terminate(component_t *c);

#ifdef __cplusplus
}
#endif

#endif /* __MODBUS_COMPONENT_H */
